# ASHS 115 Yi 語錄回憶錄網站

這是一個可直接開啟使用的靜態網站，用來整理「班上老師常講的話」。

## 如何使用

1. 直接用瀏覽器開啟 `index.html`。
2. 內容資料放在 `quotes.js`，每筆格式如下：

```js
{
  teacher: '老師名字',
  text: '語錄內容',
  context: '出現情境',
  tags: ['標籤1', '標籤2']
}
```

## 功能

- 依老師篩選
- 關鍵字搜尋（語錄、情境、標籤）
- 卡片式回顧
